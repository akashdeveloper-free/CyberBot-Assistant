services:
  - type: web
    name: cyberbot-assistant
    runtime: python
    numInstances: 1
    buildCommand: pip install -r requirements.txt
    startCommand: exec python main.py
    healthCheckPath: /health